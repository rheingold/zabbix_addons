=== AI CONTEXT - MULTIGRAPH WIDGET (PUBLIC) ====== AI CONTEXT - MULTIGRAPH WIDGET (PUBLIC) =================================================================================== AI CONTEXT - MULTIGRAPH WIDGET (GENERIC) ===

Version: 0.1.7

Updated: 2025-11-14Version: 0.1.7

Repository: https://github.com/rheingold/zabbix_addons (branch: tmp0.1)

Lead: lukas@plachy.eu | Dev: Claude Sonnet 4 (Anthropic) | License: MITUpdated: 2025-11-14MULTIGRAPH WIDGET - AI CONTEXT FILEUpdated: November 14, 2025



⚠️ CONTEXT MAINTENANCE DIRECTIVE ⚠️Repository: https://github.com/rheingold/zabbix_addons (branch: tmp0.1)

When problems are SOLVED:

1. Remove problem description from this fileLead: lukas@plachy.eu | Dev: Claude Sonnet 4 (Anthropic) | License: MIT=============================================================================Version: 0.1.6

2. Document solution in code comments or README.md

3. Add brief summary to "Last Resolved" section

4. Keep only ACTIVE/UNRESOLVED issues in "Active Issues" section

⚠️ CONTEXT MAINTENANCE DIRECTIVE ⚠️Project: Zabbix Multigraph Widget

=============================================================================

1. PROJECT OVERVIEWWhen problems are SOLVED:

=============================================================================

1. Remove problem description from this fileVersion: 0.1.7⚠️ SYNC ai.txt + ai_priv.txt | Update BOTH with: deployment/build/debug changes

PURPOSE:

Custom Zabbix 7.4.3+ dashboard widget for pattern-based multi-item graph2. Document solution in code comments or README.md

visualization using wildcards/regex instead of manual item selection.

3. Keep only ACTIVE/UNRESOLVED issues hereLast Updated: 2025-11-14This file = generic context (Git-tracked). ai_priv.txt = local paths/credentials (git-ignored).

CURRENT VERSION: v0.1.7

Status: Fully functional, supports both host and template dashboards4. Update "Last Resolved" section with brief solution summary

Known Limitations: Classic UI untested, no automated testing framework

Repository: https://github.com/rheingold/zabbix_addons (branch: tmp0.1)

REPOSITORY STRUCTURE:

/src/                    # Widget source (deploy to /usr/share/zabbix/widgets/multigraph/)=============================================================================

  manifest.json          # Widget registration, actions, version

  Widget.php             # Entry point1. PROJECT OVERVIEW⚠️⚠️⚠️ DEPLOYMENT COMMANDS: CHECK ai_priv.txt FIRST! ⚠️⚠️⚠️

  /actions/              # Controllers (WidgetView, ItemList, ModuleRescan)

  /includes/             # Models (WidgetForm, GraphData, MatchedItemsData)=============================================================================

  /views/                # Templates (widget.edit.php, widget.view.php, widget.edit.js.php)

  /assets/               # Static files (class.widget.js, multigraph.css)Lead & Architecture: lukas@plachy.euREADME.md and USAGE.md contain GENERIC examples for documentation purposes.



/ai_priv/                # Private deployment context (git-ignored)PURPOSE:

  ai_priv.txt            # Deployment commands, credentials, SSH key reference

Custom Zabbix 7.4.3+ dashboard widget for pattern-based multi-item graphDevelopment: Claude Sonnet 4 (AI Assistant, Anthropic)ACTUAL deployment commands for THIS environment are in ai_priv.txt (git-ignored).

=============================================================================

2. ACTIVE ISSUES & TODOvisualization using wildcards/regex instead of manual item selection.

=============================================================================

License: MITai_priv.txt contains: container names, TrueNAS app management, verified procedures.

PRIORITY 1 - ENHANCEMENTS:

[ ] Classic UI compatibility testingCURRENT VERSION: v0.1.7

[ ] Large dataset performance optimization (>1000 points per series)

[ ] X-axis time labels improvement (better granularity based on range)Status: Functional, production-ready for host dashboards

[ ] Item limit configurable via UI (currently hardcoded 100 in MatchedItemsData.php)

[ ] Y-axis label customizationKnown Limitations: Template dashboard support incomplete, Classic UI untested

[ ] Threshold/alert visualization

=============================================================================⚠️ CRITICAL: NO DELETES WITHOUT APPROVAL ⚠️

=============================================================================

3. LAST RESOLVED (v0.1.7 - Nov 14, 2025)REPOSITORY STRUCTURE:

=============================================================================

/src/                    # Widget source code (deploy to /usr/share/zabbix/widgets/multigraph/)PROJECT SUMMARYNEVER delete files/containers/images/code without listing & getting user confirmation first.

Template Dashboard Support:

- Problem: Pattern builder only worked with host dashboards  manifest.json          # Widget registration, actions, version

- Solution: Detect templateid field, pass to ItemList.php, use 'templateids' parameter

- Files: widget.edit.js.php (openItemSelector), ItemList.php (checkInput, doAction)  Widget.php             # Entry point=============================================================================Exception: Only ai_priv.txt/ai.txt context file recreation is auto-allowed.

- Result: Pattern builder now works in both host and template dashboards

  /actions/              # Controllers (WidgetView, ItemList, ModuleRescan)

Pattern Builder Button Not Showing:

- Problem: Button code in widget.edit.js (not loaded by Zabbix framework)  /includes/             # Models (WidgetForm, GraphData, MatchedItemsData)

- Solution: Moved to widget.edit.js.php inline class constructor

- Files: widget.edit.js.php (setupPatternBuilder method added)  /views/                # Templates (widget.edit.php, widget.view.php, widget.edit.js.php)

- Result: Button appears and functions correctly

  /assets/               # Static files (class.widget.js, multigraph.css)PURPOSE:⚠️ CONTEXT MAINTENANCE DIRECTIVE ⚠️

AJAX Endpoint 404 Error:

- Problem: widget.multigraphwidget.itemlist action not registered

- Solution: Added action mapping in manifest.json

- Files: manifest.json (actions section)/ai_priv/                # Private deployment context (git-ignored)Custom Zabbix 7.4.3+ dashboard widget for pattern-based multi-item graphWhen a problem is SOLVED:

- Result: Endpoint accessible and returns proper JSON

  ai_priv.txt            # Deployment commands, credentials

HTML Returned Instead of JSON:

- Problem: CControllerResponseData didn't work for AJAX responses  id_aibot               # SSH private keyvisualization. Enables dynamic graph creation using wildcards/regex instead1. REMOVE the problem description from ai.txt/ai_priv.txt

- Solution: Direct JSON output via header('Content-Type: application/json') + echo json_encode()

- Files: ItemList.php (doAction method)

- Result: Proper JSON response format

=============================================================================of manual item selection.2. ADD solution note to relevant file:

=============================================================================

4. ARCHITECTURE & KEY FILES2. ACTIVE ISSUES & TODO

=============================================================================

=============================================================================   - Code fixes → PHPDoc/JSDoc comment in source file where fix applied

WIDGET LIFECYCLE:

1. User opens widget config → widget.edit.php renders form HTML

2. widget.edit.js.php creates inline class extending CWidgetForm

3. setupColorSetHandler() + setupPatternBuilder() run in constructorPRIORITY 1 - IN PROGRESS:CURRENT STATUS: v0.1.7 - Pattern Builder UI Complete   - Build/deploy → Comment in Dockerfile or deployment script

4. User saves → Widget.php validates input via WidgetForm.php

5. Dashboard renders → WidgetView.php controller fetches data[ ] Template dashboard support for pattern builder

6. widget.view.php outputs HTML canvas + setVar('graph_data', ...)

7. class.widget.js (CWidgetMultigraph) renders graph on canvas    Problem: When widget is on template dashboard, hostids field empty- ✅ Core functionality (pattern matching, graph rendering)   - Architecture/design → Note in README.md relevant section



PATTERN BUILDER FLOW (v0.1.7):    Solution: Detect templateid, fetch template items via API

1. Button click → openItemSelector() detects host or template context

2. showItemList(id, isTemplate) → AJAX to widget.multigraphwidget.itemlist    Files: widget.edit.js.php (frontend), ItemList.php (backend)- ✅ Named color sets (8 predefined palettes)3. Keep context files focused on CURRENT state, not historical issues

3. ItemList.php fetches items via API::Item/ItemPrototype()->get()

   - Uses 'hostids' parameter for hosts    API: API::Item()->get(['templateids' => ...])

   - Uses 'templateids' parameter for templates

4. displayItemSelector(items) renders modal with clickable item list- ✅ Interactive pattern builder with {#MACRO} support

5. Click item → itemNameToPattern() converts {#MACROS} to */regex

6. Pattern fills item_pattern field automaticallyPRIORITY 2 - ENHANCEMENTS:



KEY API METHODS:[ ] Classic UI compatibility testing- ⚠️ Template dashboard support needed================================================================================

- API::Item()->get(['hostids'|'templateids', 'filter', 'output', 'limit'])

- API::ItemPrototype()->get(['hostids'|'templateids', 'output', 'limit'])[ ] Large dataset performance (>1000 points per series)

- API::History()->get(['itemids', 'time_from', 'time_till', 'limit'])

[ ] X-axis time labels improvement (better granularity)- ⚠️ Classic UI untestedPROJECT OVERVIEW

CRITICAL FILES:

- widget.edit.js.php: Form initialization, pattern builder, color sets[ ] Item limit configurable via UI (currently hardcoded 100)

- ItemList.php: AJAX endpoint for fetching items/prototypes

- class.widget.js: Client-side graph rendering (Canvas)- ⚠️ No automated testing framework================================================================================

- WidgetForm.php: Form field definitions and validation

- GraphData.php: History data fetching and preparation=============================================================================

- MatchedItemsData.php: Pattern matching (wildcard/regex)

3. LAST RESOLVED (v0.1.7)

=============================================================================

5. DEPLOYMENT (see ai_priv/ai_priv.txt for commands)=============================================================================

=============================================================================

=============================================================================PROJECT: Multigraph Widget for Zabbix 7.4.3+

QUICK REFERENCE:

- Single file: scp → docker cp to ix-zabbix-web-zabbix-web-1Pattern Builder Button Not Showing (Nov 14):

- Full deploy: scp src/* → docker cp to container

- Manifest change: Requires "Scan directory" in Zabbix UI- Problem: Button code in widget.edit.js (not loaded by Zabbix)COMPLETED FEATURES (v0.1.0 - v0.1.7)VERSION: 0.1.1 (Development - Untested for Classic UI)

- JavaScript change: Browser cache clear (Ctrl+F5)

- Solution: Moved to widget.edit.js.php inline class constructor

Container: ix-zabbix-web-zabbix-web-1

Widget Path: /usr/share/zabbix/widgets/multigraph/- Files: widget.edit.js.php (setupPatternBuilder method)=============================================================================STARTED: November 5, 2025



=============================================================================

6. VERSION HISTORY

=============================================================================AJAX Endpoint 404 (Nov 14):LAST UPDATED: November 10, 2025



v0.1.7 (2025-11-14) - Pattern Builder UI + Template Support- Problem: Action not registered in manifest

- Interactive Pattern Builder button in edit form

- ItemList.php AJAX endpoint (regular items + LLD prototypes)- Solution: Added "widget.multigraphwidget.itemlist" → ItemList classv0.1.7 (2025-11-14) - PATTERN BUILDER UI

- Modal selector with {#MACRO} → wildcards/regex conversion

- Template dashboard support (detects templateid, uses templateids parameter)- Files: manifest.json, ItemList.php

- Action registered: widget.multigraphwidget.itemlist

- Interactive Pattern Builder button in widget edit formTEAM:

v0.1.6 (2025-11-13) - Named Color Sets

- 8 predefined color palettes dropdownHTML Returned Instead of JSON (Nov 14):

- Integer keys (0-7) for storage efficiency

- Auto-populate graph_colors field on selection- Problem: CControllerResponseData not working for AJAX- AJAX endpoint (ItemList.php) fetches items + LLD prototypes  Lead & Architecture: lukas@plachy.eu



v0.1.5 (2025-11-12) - Module Rescan- Solution: Direct JSON output via header() + json_encode()

- Cache clearing action for hot-reload

- No web server restart needed- Files: ItemList.php (doAction method)- Modal dialog shows clickable item list with {#MACROS}  Development: Claude Sonnet 4 (AI Assistant, Anthropic)



v0.1.0 (2025-11-10) - Initial Release

- Pattern matching (wildcard/regex)

- HTML5 Canvas graph rendering=============================================================================- Auto-converts {#DISK} → * (wildcard) or .+ (regex)

- Dashboard timeframe integration

- Multi-host support4. ARCHITECTURE & KEY FILES



==========================================================================================================================================================- Validates host selection before fetchDESCRIPTION:

7. ZABBIX API QUICK REFERENCE

=============================================================================



FETCH HOST ITEMS:WIDGET LIFECYCLE:- Registered action: widget.multigraphwidget.itemlist  Custom Zabbix dashboard widget for pattern-based multi-item graph visualization.

API::Item()->get([

    'output' => ['itemid', 'name', 'key_'],1. User opens widget config → widget.edit.php renders form

    'hostids' => $hostid,

    'filter' => ['flags' => ZBX_FLAG_DISCOVERY_NORMAL],2. widget.edit.js.php creates inline class extending CWidgetForm  Instead of manual item selection, uses wildcard/regex patterns to dynamically

    'sortfield' => 'name',

    'limit' => 10003. setupColorSetHandler() + setupPatternBuilder() in constructor

]);

4. User saves → Widget.php validates via WidgetForm.phpv0.1.6 (2025-11-13) - NAMED COLOR SETS  match items by name, enabling flexible multi-metric graphs.

FETCH TEMPLATE ITEMS:

API::Item()->get([5. Dashboard renders → WidgetView.php fetches data

    'output' => ['itemid', 'name', 'key_'],

    'templateids' => $templateid,6. widget.view.php outputs HTML + setVar('graph_data', ...)- Dropdown with 8 predefined color palettes

    'sortfield' => 'name',

    'limit' => 10007. class.widget.js (CWidgetMultigraph) renders canvas graph

]);

- Integer keys (0-7) for efficient storageREPOSITORY: https://github.com/rheingold/zabbix_addons

FETCH LLD PROTOTYPES (HOST OR TEMPLATE):

API::ItemPrototype()->get([PATTERN BUILDER FLOW:

    'output' => ['itemid', 'name', 'key_'],

    'hostids' => $hostid,           // OR 'templateids' => $templateid1. Button click → openItemSelector() validates host/template- Auto-populates graph_colors field on selectionBRANCH: tmp0.1 (development)

    'sortfield' => 'name',

    'limit' => 10002. showItemList(id, isTemplate) → AJAX to widget.multigraphwidget.itemlist

]);

3. ItemList.php fetches items via API::Item/ItemPrototype()->get()- Sets: Default, Light, Vibrant, Earth, Sunset, Neon, Forest, GrayscaleLICENSE: MIT

FETCH HISTORY DATA:

API::History()->get([4. displayItemSelector(items) renders modal with clickable list

    'itemids' => [$itemid1, $itemid2],

    'time_from' => $timestamp_start,5. Click item → itemNameToPattern() converts {#MACROS}

    'time_till' => $timestamp_end,

    'history' => $value_type,        // 0=float, 3=uint64, 4=text6. Pattern fills item_pattern field

    'sortfield' => 'clock',

    'sortorder' => 'ASC',v0.1.5 (2025-11-12) - MODULE RESCANTECHNOLOGY STACK:

    'limit' => 10000

]);KEY API METHODS:



=============================================================================- API::Item()->get(['hostids'|'templateids', 'filter', 'output', 'limit'])- Added rescan action for cache clearing  Backend: PHP 8.4+ (Zabbix 7.x requirement)

END OF AI CONTEXT FILE

=============================================================================- API::ItemPrototype()->get(['hostids'|'templateids', 'output', 'limit'])


- API::History()->get(['itemids', 'time_from', 'time_till', 'limit'])- Enables hot-reload without web server restart  Frontend: JavaScript ES6+, HTML5 Canvas



=============================================================================  Framework: Zabbix Widget Framework (MVC)

5. DEPLOYMENT (see ai_priv/ai_priv.txt for commands)

=============================================================================v0.1.0 (2025-11-10) - INITIAL RELEASE  API: Zabbix API (Item, History)



QUICK REFERENCE:- Pattern matching (wildcard/regex)  Deployment: Docker (optional), SSH, PowerShell scripts

- Single file: scp → docker cp

- Full deploy: scp src/* → docker cp to container- HTML5 Canvas graph rendering

- Manifest change: Requires "Scan directory" in Zabbix UI

- JavaScript change: Browser cache clear (Ctrl+F5)- Dashboard timeframe integration================================================================================



Container: ix-zabbix-web-zabbix-web-1- Multi-host supportMAIN OBJECTIVES (D.2.1)

Path: /usr/share/zabbix/widgets/multigraph/

- Configurable graph options================================================================================

=============================================================================

6. VERSION HISTORY

=============================================================================

=============================================================================1. PATTERN-BASED ITEM SELECTION

v0.1.7 (2025-11-14) - Pattern Builder UI

- Interactive Pattern Builder button in edit formARCHITECTURE OVERVIEW   Enable users to match items using wildcards (*, ?) or regular expressions

- ItemList.php AJAX endpoint (items + LLD prototypes)

- Modal selector with {#MACRO} → */regex conversion=============================================================================   instead of manual selection, allowing dynamic graphs that auto-include new items.

- Action registered: widget.multigraphwidget.itemlist



v0.1.6 (2025-11-13) - Named Color Sets

- 8 predefined color palettes dropdownWIDGET STRUCTURE:2. MULTI-ITEM VISUALIZATION

- Integer keys (0-7) for storage efficiency

- Auto-populate graph_colors field/usr/share/zabbix/widgets/multigraph/   Display multiple matched items in single graph with customizable colors,



v0.1.5 (2025-11-12) - Module Rescan├── manifest.json          # Widget registration, actions   legend, and styling.

- Cache clearing action

- Hot-reload without web server restart├── Widget.php            # Main entry point



v0.1.0 (2025-11-10) - Initial Release├── actions/3. DASHBOARD INTEGRATION

- Pattern matching (wildcard/regex)

- HTML5 Canvas rendering│   ├── WidgetView.php    # Graph view controller   Full integration with Zabbix dashboard timeframe selector, supporting

- Dashboard timeframe integration

- Multi-host support│   ├── ModuleRescan.php  # Cache clear action   absolute, relative, and period-rounding timeframes.



=============================================================================│   └── ItemList.php      # Pattern builder AJAX endpoint

7. ZABBIX API QUICK REFERENCE

=============================================================================├── includes/4. CLIENT-SIDE RENDERING



FETCH HOST ITEMS:│   ├── WidgetForm.php    # Form field definitions   Fast HTML5 Canvas-based rendering with hover tooltips and responsive resize.

API::Item()->get([

    'output' => ['itemid', 'name', 'key_'],│   ├── GraphData.php     # History data fetching

    'hostids' => $hostid,

    'filter' => ['flags' => ZBX_FLAG_DISCOVERY_NORMAL],│   └── MatchedItemsData.php  # Item pattern matching5. ZERO ZABBIX MODIFICATIONS

    'sortfield' => 'name',

    'limit' => 1000├── views/   Self-contained widget requiring no modifications to Zabbix core files.

]);

│   ├── widget.edit.php   # Edit form HTML

FETCH TEMPLATE ITEMS:

API::Item()->get([│   ├── widget.edit.js.php # Edit form JavaScript (inline class)================================================================================

    'output' => ['itemid', 'name', 'key_'],

    'templateids' => $templateid,│   └── widget.view.php   # Display view HTMLFUNCTIONAL REQUIREMENTS (D.2.2)

    'sortfield' => 'name',

    'limit' => 1000└── assets/================================================================================

]);

    ├── js/

FETCH LLD PROTOTYPES:

API::ItemPrototype()->get([    │   ├── class.widget.js   # Main widget class (CWidgetMultigraph)D.2.2.1: Pattern Matching

    'output' => ['itemid', 'name', 'key_'],

    'hostids' => $hostid,           // or 'templateids' => $templateid    │   └── widget.edit.js    # Helper functions (not directly loaded)  - Wildcard mode: *, ? characters for simple patterns

    'sortfield' => 'name',

    'limit' => 1000    └── css/  - Regex mode: Full PCRE regex support

]);

        └── multigraph.css  - Case-sensitive matching

=============================================================================

END OF AI CONTEXT FILE  - Max 100 items matched per query (configurable)

=============================================================================

KEY CLASSES:

- CWidgetMultigraph (class.widget.js) - Client-side graph renderingD.2.2.2: Multi-Host Support

- WidgetForm (widget.edit.js.php) - Form initialization, event handlers  - Select multiple hosts

- GraphData.php - Fetches history data via API::History()->get()  - Match items across all selected hosts

- MatchedItemsData.php - Matches items via API::Item()->get()

- ItemList.php - AJAX endpoint for pattern builderD.2.2.3: Graph Rendering

  - HTML5 Canvas dual-layer architecture (base + overlay)

=============================================================================  - Time series line and area charts

DEPLOYMENT WORKFLOW  - Automatic Y-axis scaling or fixed range

=============================================================================  - Grid with configurable density

  - Legend with configurable position (top-left, right, bottom)

LOCAL DEVELOPMENT:

Windows PowerShell → SSH/SCP to remote Linux server → Docker containerD.2.2.4: Interactivity

  - Hover tooltips showing value, timestamp, series name

Commands:  - Data point interpolation for smooth hover

1. scp -i ai_priv/id_aibot ./src/* aibot@192.168.254.16:/tmp/multigraph/  - Responsive resize with automatic redraw

2. ssh -i ai_priv/id_aibot aibot@192.168.254.16 \

   "sudo docker cp /tmp/multigraph/* \D.2.2.5: Customization

   ix-zabbix-web-zabbix-web-1:/usr/share/zabbix/widgets/multigraph/"  - Configurable colors (comma-separated hex)

3. Browser: Administration → General → Modules → Scan directory  - Fill opacity (0-1)

  - Y-axis range (auto or fixed min/max)

PRODUCTION DEPLOYMENT:  - Grid enable/disable

1. Clone/pull from GitHub  - Text colors (legend, axes)

2. Copy src/* to /usr/share/zabbix/widgets/multigraph/

3. Set permissions: chmod -R 755D.2.2.6: Timeframe Support

4. Scan modules in Zabbix UI  - Absolute: Unix timestamp, ISO date strings

  - Relative: now-Xh, now-Xd, now-Xw, now-XM, now-Xy

=============================================================================  - Period rounding: now/d (today), now/w (week), now/M (month), now/y (year)

PATTERN BUILDER IMPLEMENTATION (v0.1.7)  - Integration with dashboard time selector

=============================================================================

================================================================================

ARCHITECTURE:DETAILED CONSTRAINTS (D.2.3)

Frontend: widget.edit.js.php (inline class extending CWidgetForm)================================================================================

Backend: ItemList.php (CController extending action)

Registration: manifest.json → "widget.multigraphwidget.itemlist"D.2.3.1: Platform Constraints

  - Zabbix 7.4.3+ widget framework

FRONTEND (widget.edit.js.php):  - PHP 8.4+ backend

- setupPatternBuilder() - Called from constructor  - Modern browser with HTML5 Canvas and ES6 JavaScript

- trySetupPatternBuilder() - Creates button, retries with timeouts  - Dashboard (New UI) only - Classic UI not tested

- openItemSelector() - Validates host selection

- showItemList(hostid) - AJAX call to itemlist endpointD.2.3.2: Performance Constraints

- displayItemSelector(items) - Renders modal with clickable list  - Widget load < 2 seconds (typical)

- itemNameToPattern(name) - Converts {#MACRO} → * or .+  - Max 1000 data points per series (recommended)

  - Max 100 items matched per query

BACKEND (ItemList.php):  - Canvas rendering up to 4K resolution

- checkPermissions() - Requires USER_TYPE_ZABBIX_USER

- checkInput() - Validates hostid parameterD.2.3.3: Usability Constraints

- doAction() - Fetches items via API:  - All fields validated before save

  * API::Item()->get() - Regular items  - Clear error messages

  * API::ItemPrototype()->get() - LLD prototypes with {#MACROS}  - Graceful "no data" handling

  * Merges, sorts, returns JSON  - Responsive to all dashboard cell sizes



AJAX FLOW:D.2.3.4: Security Constraints

1. Button click → openItemSelector()  - Zabbix session authentication

2. Find hostids from form multiselect (tries multiple patterns)  - Respects user host/item permissions

3. Validate at least one host selected  - Input sanitization and validation

4. showItemList(hostid) → AJAX GET zabbix.php?action=widget.multigraphwidget.itemlist  - XSS prevention in templates

5. ItemList.php returns JSON: {items: [{itemid, name, key_}, ...]}

6. displayItemSelector() renders modal with item listD.2.3.5: Browser Compatibility

7. Click item → itemNameToPattern() converts macros  - Required: HTML5 Canvas, ES6 JavaScript

8. Pattern fills item_pattern field  - Tested: Chrome 90+, Firefox 88+, Edge 90+

  - Not tested: IE11, Classic Zabbix UI, Safari

MACRO CONVERSION:

- Pattern mode 0 (wildcard): {#DISK} → *================================================================================

- Pattern mode 1 (regex): {#DISK} → .+ARCHITECTURAL CONSTRAINTS (D.2.4)

================================================================================

=============================================================================

KNOWN ISSUES & TODO1. WIDGET FRAMEWORK COMPLIANCE

=============================================================================   - Must extend Zabbix CWidget base class

   - MVC pattern: actions/ (controllers), includes/ (models), views/ (templates)

CRITICAL:   - Namespace: Widgets\Multigraph\{Actions|Includes}

- Template dashboard support: Pattern builder needs template item fetching   - PSR-4 autoloading (directory names must match namespace)

  when widget is on template dashboard (not regular host dashboard)

- Classic UI compatibility: Untested, may not work2. DATA ACCESS

- No automated testing framework   - Use Zabbix API only (no direct database access)

   - API::Item()::get() for item matching

ENHANCEMENTS:   - API::History()::get() for time series data

- Item limit not configurable via UI (hardcoded 100)

- Large dataset performance not optimized (>1000 points)3. RENDERING ARCHITECTURE

- No Y-axis label customization   - Server-side: PHP controllers prepare data

- No threshold/alert visualization   - Client-side: JavaScript renders to Canvas

   - No server-side image generation

=============================================================================   - No external JavaScript libraries (jQuery available from Zabbix)

TEMPLATE DASHBOARD SUPPORT - NEXT TASK

=============================================================================4. DEPLOYMENT ISOLATION

   - Self-contained in /usr/share/zabbix/widgets/multigraph/

PROBLEM:   - No modifications to Zabbix core files

When widget is added to template dashboard, hostids field is empty/different.   - manifest.json for registration

Pattern builder needs to detect template context and fetch:   - Scan directory for detection

- Template items (not host items)

- Template LLD rules and prototypes================================================================================

PROJECT STRUCTURE (D.2.5)

SOLUTION APPROACH:================================================================================

1. Detect template context in openItemSelector()

   - Check for templateid field instead of hostidsmultigraph/

   - Try: form.querySelector('input[name="templateid"]')├── README.md                      # Comprehensive project documentation (B.1-B.10)

2. Update ItemList.php to accept templateid parameter├── USAGE.md                       # User and admin manuals (C.1-C.2)

3. Add template item fetching:├── LICENSE                        # MIT License

   - API::Item()->get(['templateids' => $templateid])├── .gitignore                     # Git exclusions

   - API::ItemPrototype()->get(['templateids' => $templateid])├── ai.txt                         # This file - AI context (generic)

4. Pass template flag to determine context├── ai_priv/                       # Git-ignored local context

│   ├── ai_priv.txt               # Local paths, credentials, deployment details

ALTERNATIVE:│   └── id_aibot                  # SSH private key for deployment

- Check dashboard type in backend via dashboard API│

- Determine if it's template dashboard or regular├── docker/                        # Deployment helpers

- Route to appropriate item fetch method│   └── modules_rescan.php        # HTTP-accessible module rescan helper

│

=============================================================================├── zabbix-helpers.ps1            # PowerShell deployment functions

ZABBIX API REFERENCE│

=============================================================================├── deprecated/                    # Git-ignored old code (kept for reference)

│   ├── tmptest/                  # API rescan experiments

ITEM FETCHING:│   └── oldmodule/                # Old module-based approach

API::Item()->get([│

    'output' => ['itemid', 'name', 'key_'],└── src/                           # Widget source (Zabbix-compatible structure)

    'hostids' => $hostid,           // For host items    ├── manifest.json              # Widget manifest - registration metadata

    'templateids' => $templateid,   // For template items    ├── Widget.php                 # Entry point - extends CWidget

    'filter' => ['flags' => ZBX_FLAG_DISCOVERY_NORMAL],    ├── README.md                  # Source structure documentation

    'sortfield' => 'name',    │

    'limit' => 1000    ├── actions/                   # MVC Controllers

]);    │   ├── README.md

    │   ├── WidgetView.php         # Display controller (data orchestration)

ITEM PROTOTYPE FETCHING:    │   └── WidgetEdit.php         # Configuration controller

API::ItemPrototype()->get([    │

    'output' => ['itemid', 'name', 'key_'],    ├── includes/                  # MVC Models + Forms

    'hostids' => $hostid,           // For host LLD    │   ├── README.md

    'templateids' => $templateid,   // For template LLD    │   ├── GraphData.php          # History data fetching and preparation

    'sortfield' => 'name',    │   ├── MatchedItemsData.php   # Item pattern matching (wildcard/regex)

    'limit' => 1000    │   └── WidgetForm.php         # Configuration form definition

]);    │

    ├── views/                     # MVC Views / Templates

=============================================================================    │   ├── README.md

FILE CHANGE LOG    │   ├── widget.view.php        # Display template (canvas HTML)

=============================================================================    │   ├── widget.edit.php        # Configuration form template

    │   └── widget.edit.js.php     # Configuration JavaScript initialization

src/manifest.json (v0.1.7):    │

- Added "widget.multigraphwidget.itemlist" action → ItemList class    └── assets/                    # Frontend resources

- Updated version to 0.1.7        ├── js/

        │   ├── README.md

src/views/widget.edit.js.php (v0.1.7):        │   ├── class.widget.js    # Main widget JavaScript (CWidget extension)

- Added setupPatternBuilder() to constructor        │   └── widget.edit.js     # Configuration form JavaScript

- Added pattern builder methods (6 new functions)        └── css/

- Uses multiple DOM query fallbacks for form reference            ├── README.md

            └── multigraph.css     # Widget styles

src/actions/ItemList.php (NEW v0.1.7):

- AJAX endpoint for fetching items + prototypesDEPLOYMENT TARGET:

- Direct JSON output (header + echo json_encode)  /usr/share/zabbix/widgets/multigraph/

- Combines regular items + LLD prototypes

================================================================================

src/assets/js/widget.edit.js (v0.1.7):CRITICAL LESSONS LEARNED (D.1 - Kept for AI Reference)

- Contains helper methods (not directly loaded)================================================================================

- Debug logging added then removed

These are SOLVED issues - solutions documented in code comments and README.md.

README.md (v0.1.7):Kept here ONLY for AI context to avoid repeating mistakes.

- Updated version history

- Documented pattern builder feature1. WIDGET DEVELOPMENT WORKFLOW

- Updated header to v0.1.7   LESSON: Configuration fields, data flow, view templates, JavaScript rendering

           must ALL align precisely or widget fails silently.

=============================================================================   SOLUTION: Documented in README.md Section B.10 (Source Structure)

END OF AI CONTEXT FILE   CODE: See PHPDoc headers in all source files

=============================================================================

2. DATA STRUCTURE SYNCHRONIZATION
   LESSON: PHP array structure from WidgetView MUST exactly match JavaScript
           expectations in class.widget.js. Mismatches cause blank widgets.
   SOLUTION: Documented in class.widget.js::setContents() JSDoc
   CODE: WidgetView.php prepareGraphData() + class.widget.js structure comments

3. TIMEFRAME PARSING
   LESSON: Zabbix dashboard time selector uses expressions like "now/w", "now/M"
           that require special parsing logic for period rounding.
   SOLUTION: Implemented parseZabbixTime() method in WidgetView.php
   CODE: WidgetView.php lines 50-150 (parseZabbixTime method with full docs)

4. CANVAS RENDERING ORDER
   LESSON: Draw order matters: grid → fills → lines → legend. Wrong order causes
           fills to obscure lines or legend to be unreadable.
   SOLUTION: Documented in class.widget.js::renderGraph() phases
   CODE: class.widget.js lines 200-600 (13-phase rendering pipeline)

5. EVENT LISTENER CLEANUP
   LESSON: Overlay canvas event listeners accumulate on redraw, causing multiple
           tooltips. Must clone element to remove old listeners.
   SOLUTION: Documented in class.widget.js hover setup
   CODE: class.widget.js line ~550 (overlay clone logic)

6. ZABBIX FRAMEWORK CONSTRAINTS
   LESSON: Zabbix 7.4.3 has NO setHelpHint() method on form fields (unlike docs).
           Using it causes fatal errors. Must use Zabbix API only, no DB access.
   SOLUTION: Removed setHelpHint() calls, added inline comments instead
   CODE: WidgetForm.php (no setHelpHint calls, comments explain fields)

================================================================================
DEVELOPMENT WORKFLOW
================================================================================

1. LOCAL DEVELOPMENT
   - Edit files in c:\Users\plachy\Documents\Dev\Cpp\zabbix\multigraph\
   - Test locally if possible (requires Zabbix instance)
   - Git commit changes with descriptive messages

2. DEPLOYMENT TO DOCKER (see ai_priv.txt for specific paths/credentials)
   - SSH to Docker host: ssh -i ai_priv/id_aibot user@dockerhost
   - Copy files to host: scp -r src/* user@dockerhost:/tmp/multigraph/
   - Deploy to container: docker cp /tmp/multigraph zabbix-web:/usr/share/zabbix/widgets/multigraph/
   - Rebuild image: docker build -t zabbix-web-custom:latest /path/to/customzabbix
   - Restart container: docker compose restart zabbix-web

3. ENABLE WIDGET IN ZABBIX
   - Navigate to Administration → General → Modules
   - Click "Scan directory" button
   - Enable "Multigraph" widget
   - Click "Update"

4. TESTING
   - Create/edit dashboard
   - Add Multigraph widget
   - Configure (hosts, pattern, colors, etc.)
   - Verify graph renders correctly
   - Check browser console (F12) for JavaScript errors
   - Test hover tooltips, resize, timeframe changes

5. GIT WORKFLOW
   - Branch: tmp0.1 (development)
   - Commit frequently with clear messages
   - Push to origin: git push origin tmp0.1
   - Main branch reserved for v1.0.0 release

================================================================================
DEBUGGING TIPS
================================================================================

PHP ERRORS:
  - Check: /var/log/apache2/error.log or /var/log/nginx/error.log
  - Check: /var/log/php8.4-fpm.log
  - Enable debug: $DB['DB_DEBUG'] = 1 in /etc/zabbix/web/zabbix.conf.php

JAVASCRIPT ERRORS:
  - Browser console: F12 → Console tab
  - Check for: "graph_data is undefined", "querySelector returned null"
  - Verify container exists: document.querySelector('.multigraph-container')
  - Verify data structure: console.log(response.graph_data)

DEPLOYMENT ISSUES:
  - Verify files copied: ls -la /usr/share/zabbix/widgets/multigraph/
  - Check permissions: chmod -R 755, chown -R www-data:www-data
  - Verify manifest: cat manifest.json | python -m json.tool
  - Clear browser cache: Ctrl+Shift+Delete

PATTERN MATCHING ISSUES:
  - Test pattern manually: Configuration → Hosts → Items → Browser search (Ctrl+F)
  - Check case sensitivity
  - Verify regex syntax: regex101.com
  - Check Zabbix API logs for errors

PERFORMANCE ISSUES:
  - Reduce timeframe (shorter = fewer data points)
  - Reduce matched items (more specific pattern)
  - Check browser performance: F12 → Performance tab

================================================================================
CURRENT STATUS (November 14, 2025)
================================================================================

VERSION: 0.1.6 (Development)
PHASE: Active Feature Development

COMPLETED FEATURES (v0.1.6):
  ✅ Core graph rendering (line, area, bar, distribution/histogram)
  ✅ Pattern-based item selection (wildcard, regex)
  ✅ Dashboard integration (timeframe, auto-refresh)
  ✅ Interactivity (hover tooltips, resize)
  ✅ Fill rendering for line charts with missing data handling
  ✅ Distribution histogram with per-series counts
  ✅ Bar display modes (stacked/grouped)
  ✅ Named color sets with dropdown selector (8 predefined palettes)
  ✅ Default label colors changed to #909090

ACTIVE TODO:
  📋 Interactive pattern builder UI (Next task)
     - Modal popup for item prototype selection
     - Show {#MACROS}, click to select
     - Auto-convert {#DISK} to .* pattern
     - AJAX call to fetch item prototypes

PENDING:
  📋 Testing and Quality (Phase C)
  📋 Export functionality (PNG/SVG)
  📋 Performance optimization for large datasets
  📋 Classic UI compatibility testing

KNOWN ISSUES:
  - Classic UI compatibility: UNTESTED
  - Large dataset performance: NOT OPTIMIZED (> 1000 points)
  - Item limit hardcoded to 100 in MatchedItemsData.php

================================================================================
HELPERS AND TOOLS
================================================================================

COMMON COMMANDS:

**Git Operations:**
```bash
git status                                    # Check working tree status
git add .                                     # Stage all changes
git add README.md USAGE.md ai.txt             # Stage specific files
git commit -m "Update documentation"          # Commit with message
git push origin tmp0.1                        # Push to development branch
git log --oneline -n 10                       # View recent commits
git diff                                      # View unstaged changes
git diff --staged                             # View staged changes
```

**Docker Deployment (manual steps):**
```bash
# Copy files to Docker host
scp -i ~/.ssh/id_aibot -r src/* aibot@192.168.254.16:/root/customzabbix/usr/share/zabbix/widgets/multigraph/

# SSH to Docker host
ssh -i ~/.ssh/id_aibot aibot@192.168.254.16

# Build image
cd /root/customzabbix
sudo docker build -t zabbix-web-custom:latest .

# Restart container
sudo docker-compose -f /root/docker-compose.yml restart zabbix-web

# Verify deployment
sudo docker exec zabbix-web ls -la /usr/share/zabbix/widgets/multigraph/
```

**PowerShell Deployment (Windows):**
```powershell
# Copy files to Docker host
scp -i .\ai_priv\id_aibot -r .\src\* aibot@192.168.254.16:/root/customzabbix/usr/share/zabbix/widgets/multigraph/

# Build and restart via SSH
ssh -i .\ai_priv\id_aibot aibot@192.168.254.16 "cd /root/customzabbix && sudo docker build -t zabbix-web-custom:latest . && sudo docker-compose restart zabbix-web"
```

**Docker Container Inspection:**
```bash
# List containers
docker ps -a | grep zabbix

# View logs
docker logs zabbix-web
docker logs -f --tail 100 zabbix-web

# Enter container
docker exec -it zabbix-web bash

# Check files inside container
docker exec zabbix-web ls -la /usr/share/zabbix/widgets/multigraph/

# Copy logs from container
docker cp zabbix-web:/var/log/apache2/error.log ./error.log
```

**Zabbix Module Scanning:**
```bash
# HTTP helper for module information (if deployed)
curl http://192.168.254.16:8080/modules_rescan.php

# Manual: Use Zabbix UI
# Administration → General → Modules → "Scan directory" button
```

**Debugging Commands:**
```bash
# Check PHP errors (inside container or on host)
tail -f /var/log/apache2/error.log
tail -f /var/log/php8.4-fpm.log

# Search for widget errors
grep -i "multigraph" /var/log/apache2/error.log
docker logs zabbix-web 2>&1 | grep -i "multigraph"

# Verify file permissions
ls -la /usr/share/zabbix/widgets/multigraph/
stat /usr/share/zabbix/widgets/multigraph/manifest.json

# Test JSON syntax
cat manifest.json | python -m json.tool
```

**Note:** Specific deployment paths/credentials are in ai_priv.txt (git-ignored).
For complete deployment scripts, see README.md (Section B.9) and USAGE.md (Section C.2.2).

================================================================================
DOCUMENTATION REFERENCES
================================================================================

COMPREHENSIVE DOCS:
  - README.md: Project overview, architecture, requirements (B.1-B.10)
  - USAGE.md: User manual, admin manual, troubleshooting (C.1-C.2)
  - src/README.md: Source structure details
  - src/*/README.md: Directory-specific documentation

CODE DOCUMENTATION:
  - All PHP files: Comprehensive PHPDoc headers and method docs
  - All JavaScript files: Comprehensive JSDoc headers and method docs
  - All CSS files: Inline comments explaining strategies

EXTERNAL REFERENCES:
  - Zabbix Documentation: https://www.zabbix.com/documentation/current/
  - Zabbix Widget Development: Limited official docs, reverse-engineered from core
  - PHP 8.4: https://www.php.net/manual/en/
  - HTML5 Canvas: https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API
  - PCRE Regex: https://www.pcre.org/original/doc/html/pcrepattern.html

================================================================================
CURRENT TODO LIST
================================================================================
Last Updated: November 14, 2025

ACTIVE TASK:
[ ] Interactive pattern builder UI
    - Modal popup for item prototype selection
    - Show {#MACROS} from selected hosts, click to select
    - Auto-convert {#DISK} to .* pattern
    - AJAX call to fetch item prototypes

BACKLOG:
[ ] Improve X-axis time labels adaptive formatting
[ ] Export graph functionality (PNG/SVG)
[ ] Performance optimization for >1000 points per series
[ ] Automated testing framework
[ ] Classic UI compatibility

COMPLETED (v0.1.6 - November 14, 2025):
[✓] Named color sets with dropdown selector
    - 8 predefined palettes (Default, Pastel, Vibrant, Earth, Ocean, Sunset, Forest, Monochrome)
    - Integer keys (0-7) for Zabbix field compatibility
    - JavaScript handler in widget.edit.js.php constructor
    - Backend color extraction in GraphData.php
[✓] Default label colors changed to #909090
    - Changed from #000 to #909090 for better Zabbix theme integration
    - Applied to X-axis, Y-axis, and legend text
[✓] Distribution histogram per-series counts (Nov 14)
[✓] Bar display modes: stacked/grouped (Nov 14)
[✓] Fill rendering for line charts (Nov 14)
[✓] Bar chart graph type (Nov 13)
[✓] Missing data handling options (Nov 13)
[✓] Distribution/histogram graph type (Nov 13)

================================================================================
FOR AI ASSISTANTS
================================================================================

WHEN STARTING NEW SESSION:
1. Read this file (ai.txt) for project context
2. Read ai_priv.txt for local environment details
3. Review README.md for architecture and requirements
4. Check USAGE.md for user/admin operational details
5. Review relevant source files for code context

WHEN MODIFYING CODE:
1. Follow MVC pattern (actions/, includes/, views/)
2. Update PHPDoc/JSDoc headers
3. Test deployment before committing
4. Update README.md if architecture changes
5. Update USAGE.md if user-facing behavior changes
6. Clean up ai.txt/ai_priv.txt when problems are solved

WHEN ENCOUNTERING ISSUES:
1. Check "CRITICAL LESSONS LEARNED" section above
2. Review source code comments (solutions often documented inline)
3. Check README.md relevant sections
4. Review USAGE.md troubleshooting guide (C.2.4)
5. Only add to ai.txt if new lesson not already documented

WHEN PROBLEM IS SOLVED:
1. Document solution in code (PHPDoc/JSDoc comment where fix applied)
2. Document in README.md if architectural
3. Document in USAGE.md if user-facing
4. REMOVE problem description from ai.txt/ai_priv.txt
5. Keep context files focused on CURRENT state

================================================================================
END OF AI CONTEXT
================================================================================
